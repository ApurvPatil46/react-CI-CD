version: "3"

services: 
    dev:
        build: 
            context: .
            dockerfile: Dockerfile.dev
        ports: 
            - 3000:3000
        volumes:
            - /var/reactapp/node_modules 
            - .:/var/reactapp
    